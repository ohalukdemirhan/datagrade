% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{detect_outliers}
\alias{detect_outliers}
\title{Detect outliers in numeric columns}
\usage{
detect_outliers(
  data,
  numeric_columns = NULL,
  method = c("z_score", "iqr"),
  category_column = NULL,
  threshold = 3
)
}
\arguments{
\item{data}{A data frame.}

\item{numeric_columns}{Columns to test. Defaults to every numeric column.}

\item{method}{`"z_score"` (default) or `"iqr"`.}

\item{category_column}{Optional grouping column; outliers are then counted
within each group.}

\item{threshold}{Z-score cut-off. Ignored by the IQR method, which uses the
conventional 1.5 x IQR fence.}
}
\value{
A list with the total count, the share of tested values that were
  flagged, and a per-column breakdown.
}
\description{
Detect outliers in numeric columns
}
\examples{
detect_outliers(iris)$total
}

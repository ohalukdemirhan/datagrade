% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess.R
\name{dg_assess}
\alias{dg_assess}
\alias{data_quality_assessment}
\title{Assess the quality of a data set}
\usage{
dg_assess(
  data = NULL,
  path = NULL,
  outlier_method = c("z_score", "iqr"),
  imputation_method = c("mean", "median", "none"),
  correlation_threshold = 0.5,
  vif_threshold = 5,
  category_column = NULL,
  weights = c(completeness = 1, accuracy = 1, consistency = 1, timeliness = 1),
  clean_text = TRUE,
  strip_punctuation = FALSE,
  parse_dates = TRUE,
  check_duplicates = TRUE,
  anomaly_detection = FALSE,
  sample_size = 1e+05,
  seed = 1L,
  keep_data = FALSE,
  verbose = TRUE
)

data_quality_assessment(...)
}
\arguments{
\item{data}{A data frame. Either `data` or `path` must be supplied.}

\item{path}{Path to a `.csv`, `.xls` or `.xlsx` file.}

\item{outlier_method}{`"z_score"` (default) or `"iqr"`.}

\item{imputation_method}{`"mean"` (default), `"median"` or `"none"`.}

\item{correlation_threshold}{Absolute correlation above which a column pair
is reported as redundant.}

\item{vif_threshold}{Variance inflation factor above which a column counts
against the consistency score.}

\item{category_column}{Optional grouping column for outlier and correlation
analysis.}

\item{weights}{Named weights for the four dimensions in the overall score.
Dimensions that do not apply to the data are dropped and the remaining
weights renormalised.}

\item{clean_text}{Normalise whitespace in character columns.}

\item{strip_punctuation}{Also remove punctuation. Unicode-aware; off by
default because it is destructive.}

\item{parse_dates}{Convert character columns that parse cleanly as dates.
Required for the timeliness dimension to be measurable from a CSV.}

\item{check_duplicates}{Whether to run the duplicate scan, the only check
whose cost is superlinear in row count.}

\item{anomaly_detection}{Fit a random forest to flag anomalous rows.}

\item{sample_size}{Row budget for the checks that subsample.}

\item{seed}{Seed used by every subsampling step.}

\item{keep_data}{Retain the imputed data frame on the returned object.}

\item{verbose}{Print progress to the console.}

\item{...}{Passed to [dg_assess()].}
}
\value{
An object of class `dg_report`. See [print.dg_report()],
  [summary.dg_report()] and [dg_plots()].
}
\description{
Runs every check in the package and returns a `dg_report` object scoring the
data on the four ISO/IEC 25024 dimensions used in the underlying
dissertation: completeness, accuracy, consistency and timeliness.
}
\details{
The function never modifies the caller's data. Text normalisation and
imputation are applied to an internal copy used for the statistics, and the
imputed frame is returned only when `keep_data = TRUE`.

`data_quality_assessment()` is the name used in the dissertation and
  is kept as an alias.
}
\examples{
report <- dg_assess(iris, verbose = FALSE)
report$scores
summary(report)
}

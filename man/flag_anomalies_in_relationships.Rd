% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomalies.R
\name{flag_anomalies_in_relationships}
\alias{flag_anomalies_in_relationships}
\title{Flag rows whose variable relationships look anomalous}
\usage{
flag_anomalies_in_relationships(
  data,
  target = NULL,
  sample_size = 20000L,
  sd_threshold = 2,
  ntree = 100L,
  seed = 1L
)
}
\arguments{
\item{data}{A data frame.}

\item{target}{Column to predict. Defaults to the first numeric column.}

\item{sample_size}{Rows used to fit the model.}

\item{sd_threshold}{Residual cut-off in standard deviations.}

\item{ntree}{Number of trees.}

\item{seed}{Seed for both the subsample and the forest.}
}
\value{
A list with the number of anomalies, their share, and example rows;
  or `NULL` when the input has too few numeric columns.
}
\description{
Fits a random forest predicting the first numeric column from the others and
flags rows whose residual exceeds `sd_threshold` standard deviations. The
model is always fitted on a deterministic subsample, then applied to the full
data, so cost is bounded by `sample_size` rather than by the input size.
}
\details{
Requires the suggested \pkg{randomForest} package.
}
